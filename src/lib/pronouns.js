/**
 * Pronoun options offered for a player.
 *
 * Kept to the three requested; an empty value means none set, which the overlays
 * treat as "show nothing" rather than guessing.
 */
export const PRONOUN_OPTIONS = ['he/him', 'she/her', 'they/them'];
